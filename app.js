const visualsStyles=document.createElement('link');visualsStyles.rel='stylesheet';visualsStyles.href='visuals.css?v=13';document.head.appendChild(visualsStyles);
const mediaStyles=document.createElement('link');mediaStyles.rel='stylesheet';mediaStyles.href='media.css?v=13';document.head.appendChild(mediaStyles);
const heroArt=document.querySelector('.hero-art');if(heroArt){heroArt.querySelectorAll('.hero-card-main,.hero-card-float,.hero-stamp').forEach(el=>el.remove());if(!heroArt.querySelector('.hero-model-image')){const img=document.createElement('img');img.className='hero-model-image';img.src='assets/hero-modelo-janine.jpg?v=13';img.alt='Modelo do criativo Janine Fernandes';img.decoding='async';img.fetchPriority='high';heroArt.prepend(img)}}
const header=document.querySelector('[data-header]');const menuButton=document.querySelector('[data-menu-button]');const mobileMenu=document.querySelector('[data-mobile-menu]');const mobileLinks=mobileMenu?mobileMenu.querySelectorAll('a'):[];const year=document.querySelector('[data-year]');if(year)year.textContent=new Date().getFullYear();
const syncHeader=()=>{if(header)header.classList.toggle('scrolled',window.scrollY>24)};syncHeader();window.addEventListener('scroll',syncHeader,{passive:true});
if(menuButton&&mobileMenu){const setMenu=open=>{menuButton.setAttribute('aria-expanded',String(open));mobileMenu.classList.toggle('open',open);document.body.classList.toggle('menu-open',open)};menuButton.addEventListener('click',()=>setMenu(menuButton.getAttribute('aria-expanded')!=='true'));mobileLinks.forEach(link=>link.addEventListener('click',()=>setMenu(false)));window.addEventListener('keydown',event=>{if(event.key==='Escape')setMenu(false)})}
const reducedMotion=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const reveals=document.querySelectorAll('.reveal');if(reducedMotion||!('IntersectionObserver'in window)){reveals.forEach(item=>item.classList.add('visible'))}else{const observer=new IntersectionObserver((entries,instance)=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');instance.unobserve(entry.target)}}),{threshold:.12,rootMargin:'0px 0px -40px'});reveals.forEach(item=>observer.observe(item))}